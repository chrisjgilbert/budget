<%# locals: field: %>
<span id="share-<%= field.id %>" class="text-xs text-slate-500 tabular-nums w-20 text-right">
  <% if field.behavior == "shared" && field.value.to_f != 0 %>
    <%= gbp(field.share_amount) %>
  <% end %>
</span>
